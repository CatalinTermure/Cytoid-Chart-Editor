using CCE.Audio;
using UnityEngine;

namespace CCE.Timing
{
    /// <summary>
    /// Class that renders an <see cref="IAudioStream"/> as a spectrogram to a <see cref="Texture2D"/>
    /// </summary>
    public class SpectrogramRenderer : IAudioRenderer
    {
        public Texture2D RenderAudio(IAudioStream audioStream)
        {
            throw new System.NotImplementedException();
        }
    }
}
